{"python": "def load_data(filepath):\n    try:  # try to load a file, but if it doesnt exist , load an empty dictionary. (dealing with errors)\n        with open(filepath, \"r\") as f: # r for read\n            data = json.load(f)\n            return data\n    except:\n        return {}"}